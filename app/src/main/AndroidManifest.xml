<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.artic">
    <!-- 인터넷 접근 허용, 네트워크 상태 접근 허용, 와이파이 상태 접근 허용 Glide을 이용해 http에서 이미지를 받아오기 위해 필요한 권한 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".ArticApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.login.login_start.LoginStartActivity"> </activity>
        <activity android:name=".ui.signup.SignupPrivateActivity"></activity>
        <activity android:name=".ui.signup.SignupLoginActivity" />
        <activity android:name=".ui.home.artic_pick.ArticPickActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.login.login.LoginActivity">

        </activity>
        <activity android:name=".ui.detail_reading_history.DetailReadingHistoryActivity" />
        <activity android:name=".ui.detail_new_archive.DetailNewArchiveActivity" />
        <activity android:name="com.android.artic.ui.new_article_link.NewArticleLinkActivity" />
        <activity android:name=".ui.search_result.SearchResultActivity" />
        <activity android:name=".ui.search.SearchActivity" />
        <activity
            android:name=".ui.link.LinkActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.category.CategoryActivity" />
        <activity android:name=".ui.home.HomeActivity"></activity>
        <activity android:name=".ui.archive.ArchiveActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>